{
  "name": "@tm-forum/tmf673-geographic-address",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@tm-forum/database": "workspace:*",
    "@tm-forum/shared": "workspace:*",
    "@tm-forum/tmf-common": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.18",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1"
  }
}
